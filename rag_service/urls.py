"""
URL configuration for RAG Service
"""

from django.urls import path
from . import views

app_name = "rag_service"

urlpatterns = [
    # Add URL patterns here if needed
    # path("community/", views.CommunitySummaryView.as_view(), name="community"),
]

